<Window x:Class="PrisomObservesSample.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:PrisomObservesSample"
        mc:Ignorable="d"
        Title="MainWindow" Height="140" Width="800">
    <Window.DataContext>
        <local:MainWindowViewModel/>
    </Window.DataContext>
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition/>
            <ColumnDefinition Width="Auto"/>
            <ColumnDefinition/>
            <ColumnDefinition Width="Auto"/>
            <ColumnDefinition/>
        </Grid.ColumnDefinitions>

        <!-- 入力1 -->
        <TextBox Grid.Row="0" Grid.Column="0" Height="30" Margin="5"
                 FontSize="18"
                 Text="{Binding Input1}"/>
        <!-- 入力2 -->
        <TextBox Grid.Row="0" Grid.Column="2" Height="30" Margin="5"
                 FontSize="18"
                 Text="{Binding Input2}"/>
        <!-- 結果 -->
        <TextBlock Grid.Row="0" Grid.Column="4" Height="30" Margin="5"
                   FontSize="18"
                   Text="{Binding Result}"/>

        <TextBlock Grid.Row="0" Grid.Column="1"
                   TextAlignment="Center" VerticalAlignment="Center"
                   FontSize="40" Text="＋"/>
        <TextBlock Grid.Row="0" Grid.Column="3"
                   TextAlignment="Center" VerticalAlignment="Center"
                   FontSize="40" Text="＝"/>

        <Button Grid.Row="1" Grid.Column="4" Margin="4"
                Content="計算"
                Command="{Binding SumCommand}"/>
    </Grid>
</Window>
